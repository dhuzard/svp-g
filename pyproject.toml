[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "svp"
version = "1.2.1"
description = "Stratified Verification Pipeline - a Claude Code plugin for deterministically orchestrated software development"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}
authors = [
    {name = "Carlo Fusco"},
    {name = "Leonardo Restivo"},
]
dependencies = ["mcp"]

[project.scripts]
svp = "svp.scripts.svp_launcher:main"
svp-mcp = "svp_mcp.server:main"

[tool.setuptools.packages.find]
where = ["src", "."]
include = ["svp*", "svp_core*", "svp_host_claude*", "svp_app*", "svp_mcp*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
